import { useAuth } from "./authContext";
import { useScreenSize } from "./screenSizeContext";
import { useCheckoutDisabled } from "./checkoutDisabledContext";

const Contexts = {
	useAuth,
	useScreenSize,
	useCheckoutDisabled,
};

export default Contexts;
